interface:
  display_name: "Cloud Aikido"
  short_description: "Repo-specific patterns and workflows for cloud-aikido"
  default_prompt: "Use the cloud-aikido repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true